# Realistic small-model experiment after the pipeline is stable.
vocab_size: 32000
# The 1024 context experiment increases activation memory substantially.
context_length: 1024
n_layers: 12
n_heads: 12
embedding_dim: 768
